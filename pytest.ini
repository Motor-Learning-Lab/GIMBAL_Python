[pytest]
# GIMBAL Test Configuration

# Only collect tests from these paths
testpaths = tests

# Exclude these directories from test collection
norecursedirs = tests/diagnostics tests/debug debug __pycache__ .ipynb_checkpoints *.egg-info

# Always show test summary info (passed, failed, errors, warnings)
addopts = -ra
